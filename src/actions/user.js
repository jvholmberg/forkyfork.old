// import axios from 'axios';

export const getUser = (state) => {
  console.log(state);
  const mockObj = {
    username: 'Username',
    firstName: 'Firstname',
    lastName: 'LastName',
  };
  return mockObj;
};

export const registerUser = (state) => {
  console.log(state);
  const mockObj = {
    username: 'Username',
    firstName: 'Firstname',
    lastName: 'LastName',
  };
  return mockObj;
};
