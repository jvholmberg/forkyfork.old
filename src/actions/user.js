// import axios from 'axios';

export function getUserDetails() {
  const mockObj = {
    firstname: 'Firstname',
    lastName: 'LastName',
  };
  return mockObj;
}

export function registerUser() {
  const mockObj = {
    firstname: 'Firstname',
    lastName: 'LastName',
  };
  return mockObj;
}
